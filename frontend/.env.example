# ─── API ────────────────────────────────────────
# Backend API URL (localhost for dev, Render URL for prod)
NEXT_PUBLIC_API_URL=http://localhost:4000

# ─── WebSocket ──────────────────────────────────
# WebSocket URL (same as API in dev, separate in prod)
NEXT_PUBLIC_WS_URL=http://localhost:4000
